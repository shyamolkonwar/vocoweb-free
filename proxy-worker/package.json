{
    "name": "proxy-worker",
    "version": "1.0.0",
    "description": "Cloudflare Worker to proxy subdomain traffic to Pages branches",
    "main": "src/index.js",
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}